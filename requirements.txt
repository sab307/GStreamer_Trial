# =============================================================================
# Python dependencies — GStreamer WebRTC Sender
# Install with:  pip install -r requirements.txt
# =============================================================================

# WebSocket client used by webrtc_sender.py to connect to the Go signaling server
websockets>=11.0

# Numerical operations in ros2_bridge.py (image frame conversion)
numpy>=1.21

# Python GObject / GStreamer bindings (gi.repository.Gst, GstWebRTC, GstSdp, GLib)
# NOTE: PyGObject requires the system GLib/GStreamer dev headers to build.
#       On Ubuntu/Debian, install system packages first (see below) then pip-install this.
PyGObject>=3.42.0


# =============================================================================
# System packages (apt) — required BEFORE pip installing the above
# =============================================================================
#
#   sudo apt install -y \
#       python3-gi \
#       python3-gi-cairo \
#       gir1.2-gst-1.0 \
#       libglib2.0-dev \
#       libgirepository1.0-dev \
#       libcairo2-dev \
#       pkg-config \
#       gstreamer1.0-tools \
#       gstreamer1.0-plugins-base \
#       gstreamer1.0-plugins-good \
#       gstreamer1.0-plugins-bad \
#       gstreamer1.0-plugins-ugly \
#       gstreamer1.0-libav \
#       libgstreamer1.0-dev \
#       libgstreamer-plugins-base1.0-dev


# =============================================================================
# ROS2 dependencies (optional — only needed when --simulate is NOT used)
# =============================================================================
# rclpy and sensor_msgs are installed as part of a ROS2 distribution, not via pip.
# Follow the official ROS2 installation guide for your distro:
#   https://docs.ros.org/en/rolling/Installation.html
#
# Required ROS2 packages:
#   ros-<distro>-rclpy
#   ros-<distro>-sensor-msgs
#   ros-<distro>-cv-bridge        (if using compressed image topics)
#
# Source your ROS2 workspace before running:
#   source /opt/ros/<distro>/setup.bash


# =============================================================================
# Go (signaling server) — NOT a Python dependency
# =============================================================================
# The signaling server (signaling/) is written in Go.
# Install Go >= 1.21: https://go.dev/doc/install
#
# The only Go dependency is declared in signaling/go.mod:
#   github.com/gorilla/websocket v1.5.3
#
# Install it with:
#   cd signaling && go mod download
#
# Run with:
#   cd signaling && go run . --port 8089
